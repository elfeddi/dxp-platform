app:
  title: DxP Developer Portal
  baseUrl: http://localhost:7007

  # Enable all packages by default, this will discover packages from packages/app/package.json
  packages: all

  extensions:
    # Disable the nav items that we're manually rendering in packages/app/src/modules/nav/Sidebar.tsx
    - nav-item:search: false
    - nav-item:user-settings: false
    - nav-item:catalog: false
    - nav-item:scaffolder: false

    # Configure the catalog index page to be the root page, this is normally mounted on /catalog
    - page:catalog:
        config:
          path: /

organization:
  name: DxP

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  # https désactivé en mode dev — Backstage écoute en HTTP sur 7007
  # https:
  #   certificate:
  #     cert:
  #       $file: certs/cert.pem
  #     key:
  #       $file: certs/key.pem
    # Uncomment the following host directive to bind to specific interfaces
    host: 0.0.0.0
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    worker-src: ["'self'", 'blob:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: http://158.158.8.131:7007
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  database:
    client: better-sqlite3
    connection:
      directory: /home/azureuser/dxp-platform/dxp-portal/.backstage-db
  # see https://backstage.io/docs/ai/mcp-actions#actions-configuration for more details
  actions:
    pluginSources:
      - auth
      - catalog
      - scaffolder
  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir

integrations:
  github:
    - host: github.com
      token: ${GITHUB_TOKEN}

proxy:
  endpoints:
    '/dxp':
      target: 'http://localhost:30890'
      changeOrigin: true
      credentials: dangerously-allow-unauthenticated
      headers:
        Authorization: 'Bearer viewer'
    '/dxp-admin':
      target: 'http://localhost:30890'
      changeOrigin: true
      credentials: dangerously-allow-unauthenticated
      headers:
        Authorization: 'Bearer operator' 

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    runIn: 'docker' # Alternatives - 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.

auth:
  # see https://backstage.io/docs/auth/ to learn about auth providers
  providers:
    # See https://backstage.io/docs/auth/guest/provider
    guest:
      dangerouslyAllowOutsideDevelopment: true
  # see https://backstage.io/docs/ai/mcp-actions#client-id-metadata-documents
  # to learn more about client id metadata documents
  experimentalClientIdMetadataDocuments:
    enabled: false
    # Optional: restrict which `client_id` URLs are allowed (defaults to ['*'])
    # allowedClientIdPatterns:
    #   - 'https://example.com/*'
    #   - 'https://*.trusted-domain.com/*'
    # Optional: restrict which redirect URIs are allowed (defaults to ['*'])
    # allowedRedirectUriPatterns:
    #   - 'http://localhost:*'
    #   - 'https://*.example.com/*'

scaffolder:
  # see https://backstage.io/docs/features/software-templates/configuration for software template options

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location, Template]
  locations:
    - type: url
      target: https://github.com/elfeddi/dxp-platform/blob/main/templates/golden-path-devops/template.yaml
      rules:
        - allow: [Template]
    - type: url
      target: https://github.com/elfeddi/dxp-platform/blob/main/stack0/catalog-info.yaml
      rules:
        - allow: [Component]

kubernetes:
  serviceLocatorMethod:
    type: multiTenant
  clusterLocatorMethods:
    - type: config
      clusters:
        - url: https://0.0.0.0:41745
          name: dxp-poc
          authProvider: serviceAccount
          skipTLSVerify: true
          serviceAccountToken: eyJhbGciOiJSUzI1NiIsImtpZCI6InpWbG9mWjZSeXNycEZKT3JHc1k3cl9zQmtjUUlrVUNtckFvNV92SU9xMHMifQ..b4a5wWBKDMdaM-UkZy99HR26cgrs4bvi7WBVtYzBkZINxkwnhBMVbIyTlsfzk7YKvHUwaZlW8DEsfBKoSdj7D04NUqkvVmfX7wbcUHhLRBhYMyBB_AYm0WZM_FKy5yYq1FnE9d6v6b5IirbCVwYswU9dxe7TSbTjUCa29sKeFXHDk-fh4WHjSMkmcb1_FgBBX-cfb4Pn4s1k3jVZ_JNaxSGn8sEzdV1gBxbpG4Ixqn5cVx_CBSlLGJCao0heZI1TR7-eDcOFRMpJ_HAkINwGasw1PTksnnebD7u3kOfUk343IQvLM7t8H83l4e6kszEm0OA0YgnI_griLA9vimbHIQ

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: false

# see https://backstage.io/docs/ai/mcp-actions for more details
mcpActions:
  name: 'DxP Developer Portal' # defaults to "backstage"
  description: 'Tools for managing your software catalog, creating new services from templates, and exploring your developer portal' # optional


argocd:
  appLocatorMethods:
    - type: 'config'
      instances:
        - name: argocd
          url: http://argocd-server.argocd.svc.cluster.local
          token: ${ARGOCD_AUTH_TOKEN}
